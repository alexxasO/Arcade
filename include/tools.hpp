/*
** EPITECH PROJECT, 2021
** arcade
** File description:
** tools
*/

#ifndef TOOLS_H_
#define TOOLS_H_

#define MAX(a, b) ((a < b) ? (b) : (a))
#define MIN(a, b) ((a < b) ? (a) : (b))
#define ABS(a) ((a < 0) ? (-a) : (a))

#endif /* !TOOLS_H_ */
